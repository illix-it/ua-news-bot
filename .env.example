# Required
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Safety switches
MAX_POSTS_PER_RUN=1
DRY_RUN=true
AI_ENABLED=false

# Polling (24/7 mode)
POLL_INTERVAL_SECONDS=60

# Warm start: mark current RSS backlog as seen
INIT_SKIP_EXISTING=true

# During warm start, optionally post/preview the latest item (0/1)
INIT_POST_LATEST=true