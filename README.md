# ua-news-bot

Telegram aggregator for Ukrainian political news from external sources
(Ukrinform, Suspilne).  
Pipeline: RSS / website → processing → publishing to Telegram via Bot API.

## Requirements
- Python 3.12+
- uv

## Setup
1) Install dependencies:
```bash
uv sync
